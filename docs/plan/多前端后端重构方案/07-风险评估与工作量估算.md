# 四、风险评估 + 五、工作量估算

> 融合方案1的风险分类（含CGCXX拆分专项）+方案0的性能量化数据+Phase8交互矩阵

---

## 四、风险评估

### 4.1 编译/测试回归风险

| 风险 | 概率 | 影响 | 缓解措施 | 检测方法 | 量化指标 |
|------|------|------|---------|---------|---------|
| IR转换引入语义差异 | 中 | 高 | 每个Task完成后对比BTIR→LLVM IR与现有CodeGen输出 | 差分测试：新路径vs旧路径编译结果对比 | 差分测试通过率100%；目标代码二进制差异=0 |
| C++特有语义丢失(异常/RTTI/dynamic_cast) | 中 | 高 | Phase B中C++特有Task逐一验证 | C++特性专项测试：异常/RTTI/dynamic_cast/virtual | C++特性测试覆盖率100%；每个特性≥3个测试用例 |
| 测试覆盖不足 | 低 | 中 | 每个Phase新增IR层单元测试+端到端测试 | 代码覆盖率工具：lcov/llvm-cov | 代码行覆盖率≥80%；IR层覆盖率≥90% |
| IR指令遗漏导致某些C++特性无法表达 | 中 | 高 | 逐指令对照LLVM IR指令集；增量测试 | 指令覆盖率统计：LLVM IR指令→BTIR指令映射表 | LLVM IR指令映射覆盖率≥95%；未映射指令有降级方案 |

### 4.2 性能影响评估（取自方案0量化数据）

| 方面 | 影响 | 分析 |
|------|------|------|
| 编译速度 | 轻微下降（~3-8%） | 引入IR中间层增加一次内存拷贝；IR→LLVM IR转换有开销 |
| 代码质量 | 无影响 | 最终仍使用LLVM优化管线，生成代码质量不变 |
| 内存使用 | 增加（~10-20%） | BTIR和LLVM IR同时存在于内存 |
| 二进制大小 | 增加（~5%） | IR库代码增加 |

**缓解**：Phase E 后可优化 IR→LLVM IR 转换为流式处理，减少内存峰值。

### 4.3 CGCXX 拆分风险（取自方案1专项）

| 风险 | 概率 | 影响 | 缓解措施 | 检测方法 | 量化指标 |
|------|------|------|---------|---------|---------|
| 拆分引入编译错误 | 低 | 低 | 先物理拆分（仅分文件），逻辑不变 | 编译测试：拆分前后编译结果一致 | 拆分后0个新增编译错误 |
| VTable生成迁移到后端 | 中 | 高 | 保留CGVTable.cpp在CodeGen中，Phase C迁移到LLVM后端 | VTable专项测试：虚函数调用结果正确 | VTable测试用例≥10个；所有虚函数调用正确 |
| 多重继承语义丢失 | 中 | 高 | 专项测试：多重继承、虚继承、thunk | 继承测试矩阵：单一/多重/虚继承×thunk | 继承测试用例≥15个；覆盖所有继承模式 |
| 类型映射不一致 | 中 | 中 | IRTypeMapper和IRToLLVMConverter双向测试 | 双向测试：QualType→IRType→llvm::Type往返一致 | 往返映射一致率100% |

### 4.4 与 Phase 8 工作的兼容性

| Phase 8 工作 | 兼容方案 |
|-------------|---------|
| TargetInfo/ABIInfo | TargetLayout镜像TargetInfo布局信息；ABIInfo移入后端层 |
| Mangler substitution | IRMangler复用substitution逻辑，不依赖CodeGenModule |
| Builtins | Builtins属于前端语义，在ASTToIRConverter中处理 |
| 调用约定 | 调用约定作为IR函数属性，LLVM后端映射到LLVM CallingConv |
| 优化pipeline | LLVM优化pipeline在LLVMBackend中复用，不受影响 |
| 目标特定优化 | 目标特定优化在LLVMBackend中执行，不受影响 |

### 4.5 其他风险

| 风险 | 概率 | 影响 | 缓解措施 | 检测方法 | 量化指标 |
|------|------|------|---------|---------|---------|
| 新旧路径并存导致维护负担 | 低 | 中 | 限制并存时间；尽快完成Phase E | 代码统计：新旧路径共存的代码行数 | 共存代码行数≤总代码5%；Phase E后共存代码=0 |
| IR设计不够通用，后续前端/后端扩展受限 | 低 | 高 | IR指令集参考LLVM IR和MLIR，保持通用性 | 扩展性测试：新增Dialect/Pass的代码量 | 新增Dialect代码量≤200行；新增Pass代码量≤100行 |
| IR序列化格式不稳定 | 低 | 低 | 初期文本格式为主，二进制格式后续优化 | 版本兼容性测试：旧版本IR文件加载 | IRFormatVersion兼容性检查100%覆盖 |
| 团队学习成本 | 中 | 中 | 提供详细文档和示例；渐进式引入 | 文档完整性检查：每个新API都有文档和示例 | API文档覆盖率≥95%；每个新模块≥1个示例 |

### 4.6 缺失项补充带来的新增风险

| 风险 | 概率 | 影响 | 缓解措施 | 检测方法 | 量化指标 |
|------|------|------|---------|---------|---------|
| BumpPtrAllocator 与 IRModule unique_ptr 所有权冲突 | 低 | 中 | 明确职责划分：IRContext 拥有节点内存，IRModule 拥有逻辑结构 | 代码审查：检查IRModule是否直接delete IR节点；AddressSanitizer检测double-free | AddressSanitizer报告0个double-free/use-after-free |
| IRConversionResult 错误恢复导致无效 IR 传递给后端 | 低 | 高 | 契约验证（B.4.1）在管线中拦截无效 IR | 契约验证函数返回false时检查IRModule内容 | contract::verifyAllContracts()对含错误的IRModule返回false，100%拦截 |
| BackendCapability 声明不完整导致运行时崩溃 | 低 | 高 | LLVM 后端声明全部特性；新增后端必须声明最低特性集 | 运行时检查：IRModule::getRequiredFeatures()与BackendCapability对比 | getUnsupported()返回0表示完全支持；非零时编译中止 |
| IRFormatVersion 兼容性检查遗漏 | 低 | 中 | IRReader 加载时强制检查版本号 | 单元测试：使用旧版本IR文件加载，验证拒绝 | IRReader加载Major不匹配的文件→返回错误，100%拒绝 |
| IR Pass（ConstantFolding/DeadFuncElim）引入语义变化 | 中 | 中 | Pass 后运行 VerifierPass 验证 | 差分测试：Pass前后IR语义等价 | DifferentialTester::test()→IsEquivalent==true |

### 4.7 行业前沿增强新增风险

> 来源：09-行业前沿补充优化方案 §4.1

| 风险ID | 风险描述 | 严重程度 | 概率 | 影响范围 | 缓解措施 | 检测方法 | 量化指标 |
|--------|---------|---------|------|---------|---------|---------|---------|
| R-F01 | Dialect机制引入导致IR指令体积增大（每条指令+1字节DialectID） | 低 | 高 | IR内存占用 | DialectID使用uint8_t（1字节），影响<1%；可通过压缩编码进一步优化 | 内存基准测试：对比有无DialectID的IRModule内存占用 | sizeof(IRInstruction)增加≤1字节；总内存增加<1% |
| R-F02 | DialectLoweringPass降级逻辑复杂，C++语义丢失可能导致后端行为差异 | 高 | 中 | 多后端一致性 | 差分测试(D-F7)验证降级前后语义等价；每个降级规则必须有对应测试 | 差分测试：DialectLoweringPass前后IR语义等价 | DifferentialTester::test()→IsEquivalent==true；降级规则测试覆盖率100% |
| R-F03 | 查询式编译架构引入导致编译器内部架构复杂度显著增加 | 中 | 高 | Phase F/G开发周期 | 渐进式引入：Phase F仅基础查询，Phase G增量编译，Phase H自动增量 | 代码复杂度度量：查询模块圈复杂度 | 查询模块圈复杂度≤30/函数；Phase F仅实现5个基础查询 |
| R-F04 | 插件系统动态加载可能导致编译器稳定性问题 | 中 | 中 | 编译器运行时 | 插件版本兼容性检查；插件崩溃不影响主编译器 | 插件崩溃测试：加载恶意插件后主编译器仍正常 | 插件崩溃→主编译器不受影响；插件兼容性检查覆盖率100% |
| R-F05 | AI自动调优模型质量不可控，可能推荐错误Pass序列 | 中 | 低 | 优化质量 | AI调优仅作为建议，不自动执行；保留默认Pass序列作为基线 | 对比测试：AI推荐序列vs默认序列 | AI推荐序列性能≥默认序列的90%；最差情况回退到默认序列 |
| R-F06 | IR模糊测试发现大量现有bug，可能延迟Phase进度 | 中 | 中 | 开发节奏 | 模糊测试在Phase D引入，发现的bug按优先级处理；不阻塞Phase主线 | 模糊测试结果统计：每Phase发现的bug数 | 模糊测试发现的P0 bug≤5个/Phase；P1+P2 bug不阻塞主线 |
| R-F07 | FFI跨语言互操作调用约定差异导致ABI兼容性问题 | 高 | 中 | 跨语言调用 | 优先支持C ABI（零开销）；其他语言通过C ABI桥接 | FFI调用测试：跨语言调用结果与直接调用一致 | C ABI调用100%正确；非C ABI调用测试覆盖率≥80% |
| R-F08 | DWARF5调试信息生成复杂度高，可能影响编译速度 | 低 | 中 | 编译性能 | 调试信息生成可选(-g)；非调试构建零开销 | 编译速度基准：-g vs 无-g | -g编译速度降低≤15%；非调试构建零性能影响 |
| R-F09 | 远程编译缓存安全性（缓存投毒/中间人攻击） | 高 | 低 | 分布式编译 | 远期实现；缓存条目签名验证；HTTPS传输 | 安全审计：缓存条目签名验证 | 缓存条目签名验证100%覆盖；HTTPS传输加密 |
| R-F10 | 新增工作量65.5人日导致项目周期延长 | 中 | 高 | 项目管理 | 新增任务按优先级排序；Phase A-E新增任务为附加增强，不影响主线 | 里程碑追踪：每个Phase实际工期vs计划工期 | 每Phase工期偏差≤20%；P0任务100%按时完成 |

---

## 五、工作量估算

### 5.1 各 Phase 工作量

| Phase | 内容 | 基础工作量 | 风险系数 | 调整后 |
|-------|------|-----------|---------|--------|
| A | IR基础设施（含IRContext+IRThreadingMode+IRFormatVersion） | 14.5天 | 1.0 | 14.5天 |
| B | 前端抽象层+C++前端适配（含错误恢复+IR Pass+契约测试） | 24天 | 1.2 | 29天 |
| C | 后端抽象层+LLVM后端适配（含BackendCapability） | 17天 | 1.15 | 20天 |
| D | 编译管线重构 | 6天 | 1.1 | 7天 |
| E | 验证和迁移 | 7天 | 1.15 | 8天 |
| **总计** | | **68.5天** | | **78.5天** |

> 说明：方案0估算28-40天（过乐观），方案1估算74天（含1.3风险系数偏高）。融合取中间值，风险系数按Phase难度分别设定。
> 缺失项补充新增约 6.5 天基础工时（A+2.5天, B+7.5天, C+1天），含风险系数后新增约 8.5 天。

### 5.1.1 附加增强工作量（行业前沿补充）

> 来源：09-行业前沿补充优化方案 §4.2

| Phase | 附加增强工作量 | 说明 |
|-------|-------------|------|
| Phase A | +5.5人日 | DialectID/DbgInfo/Telemetry/CacheKey/Checksum基础定义 |
| Phase B | +18人日 | DialectLoweringPass/诊断增强/缓存/不变量检查/调试信息 |
| Phase C | +11.5人日 | 插件系统/FFI/模糊测试/SARIF/FixIt |
| Phase D | +15.5人日 | InstructionSelector/RegisterAllocator/DWARF5/差分测试/模糊测试 |
| Phase E | +15人日 | 编译缓存集成/等价性检查/可重现构建/FFI/CodeView |
| Phase F | 约30人日（远期） | 查询式编译/声明式指令选择/插件注册/AI预留/远程缓存 |
| Phase G | 约25人日（远期） | 增量编译红绿标记/缓存持久化/投影查询 |
| Phase H | 约40人日（远期） | AI调优/自动增量/安全增强/分布式缓存/形式化验证 |
| **附加增强总计** | **+65.5人日(A-E) + 95人日(F-H)** | **含远期共+160.5人日** |

### 5.1.2 建议优先级排序

> 来源：09-行业前沿补充优化方案 §4.2

| 优先级 | 任务类别 | Phase | 理由 |
|--------|---------|-------|------|
| P0（必须） | DialectID字段 + DbgInfo字段 + Telemetry基础 | A | 架构基础，后续所有增强依赖 |
| P0（必须） | DialectLoweringPass | B | 多后端解耦核心机制 |
| P0（必须） | InstructionSelector + RegisterAllocator接口 | D | 后端模块化核心 |
| P1（重要） | 结构化诊断 + FixIt | B-C | IDE集成必需 |
| P1（重要） | 编译缓存 | B-E | 编译速度提升 |
| P1（重要） | 差分测试 + IR验证 | D-E | 编译器质量保证 |
| P2（增强） | 插件系统 | C-F | 可扩展性 |
| P2（增强） | FFI接口 | C-E | 跨语言互操作 |
| P2（增强） | 查询式编译 | F-G | 增量编译基础 |
| P3（远期） | AI自动调优 | H | 前沿探索 |
| P3（远期） | 形式化验证 | H | 正确性极致保证 |

**含附加增强后总计工作量**：约 **78.5天（A-E主线） + 65.5人日（A-E附加） + 95人日（F-H远期） ≈ 285.5人日**

### 5.2 执行顺序和并行策略

**严格串行**（因红线R3要求每阶段可编译可测试）：

```
Phase A (14.5天) → Phase B (29天) → Phase C (20天) → Phase D (7天) → Phase E (8天)
```

**Phase 内部并行**（取自方案0）：
- Phase A：A.1-A.4 可部分并行（类型/值/模块可同时开发）
- Phase B：B.1-B.2 可与 B.3 并行；B.4-B.6 必须串行
- Phase C：C.1 可与 C.2 并行；C.3-C.8 必须串行

**Phase B/C 并行机会**（取自方案0，可缩短总工期约5-7天）：
- Phase B 的 Task B.1-B.2（前端接口+基础转换）与 Phase C 的 Task C.1（后端接口）可并行
- Phase B 的 Task B.4-B.6（表达式/语句/C++ 特有）与 Phase C 的 Task C.2（IR→LLVM 转换）可部分并行

**考虑并行后总工期**：约 **71天**

### 5.3 关键路径

```
Phase A → Phase B (CGCXX 拆分) → Phase D → Phase E
```

CGCXX 拆分（Task B.7-B.8，5天）是整个项目的关键瓶颈。

### 5.4 关键里程碑

| 里程碑 | 时间 | 验收标准 |
|--------|------|---------|
| M1: IR库可用 | Phase A完成后 | libblocktype-ir编译通过，所有IR类型/指令/验证器测试通过 |
| M2: C++前端可产出IR | Phase B完成后 | CppFrontend可将C++源文件编译为BTIR |
| M3: LLVM后端可消费IR | Phase C完成后 | LLVMBackend可将BTIR编译为目标文件 |
| M4: 新管线可用 | Phase D完成后 | `--frontend=cpp --backend=llvm` 可完整编译 |
| M5: 迁移完成 | Phase E完成后 | 旧路径移除，全部功能通过新架构 |
